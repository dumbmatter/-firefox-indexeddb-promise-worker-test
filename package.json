{
  "name": "firefox-indexeddb-promise-worker-test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rm -rf build && mkdir build && cp *.html build && npm run build-js",
    "build-js": "browserify app.js -o build/app.js",
    "start": "npm run build && node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browserify": "^14.1.0",
    "express": "^4.15.2",
    "immediate": "^3.2.3",
    "lie": "^3.1.1"
  }
}
